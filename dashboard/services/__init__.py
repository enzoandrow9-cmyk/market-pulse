# services/  —  Market Pulse data service layer
# Each module wraps a specific data source and exposes a clean, cached API.
# Dash callbacks should call these services instead of raw yfinance / requests.
